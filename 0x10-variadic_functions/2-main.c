#include "variadic_functions.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	print_strings(", ", 9, NULL, NULL, NULL, "Jay", NULL, "Django", NULL, NULL, NULL);
	return (0);
}
